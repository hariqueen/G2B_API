# 최종 기획 — 사전규격·발주계획 대시보드 반영

**2026-08-04** · 근거 스펙: [조달청API_발주계획_사전규격_스펙.md](./조달청API_발주계획_사전규격_스펙.md)

> 이 문서의 모든 수치는 **실제 API 호출과 운영 RTDB 조회로 실측**한 값이다. 추정치가 아니다.
> 검토 과정에서 초안의 가정 두 개가 실측에 의해 뒤집혔다. 해당 항목은 본문에 **[정정]** 으로 표시했다.

---

## 1. 요약

| | 내용 |
|---|---|
| **하는 것** | ① 사전규격 목록 신설 ② 의견마감 알림 ③ 공고 양방향 연결 ④ 예정입찰 위젯 개편 |
| **배치** | 각 도메인 **하위 서브탭** (입찰공고 목록과 분리) |
| **발주계획** | 별도 탭 아님. 사전규격 목록에 **소스 병합** |
| **핵심 가치** | 의견등록 **5일 창** — 발주 규격을 바꿀 수 있는 유일한 공식 창구 |
| **API 부담** | 일 32건 (한도 2,000건의 1.6%) |

---

## 2. 왜 하는가

기존 대시보드는 **입찰공고**만 본다. 공고가 뜬 시점엔 이미 경쟁이 시작된 뒤다.

| 지표 | 실측 |
|---|---|
| 사전규격 접수 → 입찰공고 리드타임 | **중앙값 8일** (325쌍 중 **100%** 선행) |
| 의견등록 마감까지 | 접수일로부터 **중앙값 5일** |

**두 가지를 얻는다.**

1. **8일 먼저 안다** — 검토·준비를 앞당긴다
2. **규격에 개입할 수 있다** — 의견등록은 발주 규격을 바꿀 수 있는 **유일한 공식 창구**다

두 번째가 본질이다. 조기 탐지는 8일 이득이지만, 의견 반영은 **입찰 조건 자체**를 바꾼다. 그 창이 평균 5일만 열린다.

---

## 3. 실측 데이터

대시보드 등록 키워드 13개 · 용역 · 최근 365일 기준.

| 지표 | 사전규격 | 발주계획 |
|---|---|---|
| 건수 | **271건** (월 22.6건) | **346건** |
| `bidNtceNoList` 보유 | 249건 (**91%**) | 241건 (69%) |
| RTDB 조인 성공 | 325/325 (**100%**) | 297/297 (**100%**) |
| 입찰까지 리드타임 | 중앙값 **8일** | 중앙값 7일 |
| 미공고 상태 | — | 105건 (30%) |
| 상대편에 없는 고유 건 | 65개 | **37개** |

- SW사업 대상 65건(23%) · 배정예산 중앙값 2.28억(합계 3,380억)
- 키워드별: 콜센터 152 · 상담센터 32 · 상담시스템 32 · 고객센터 31 · … · **민원센터 0**
- 상시 "의견등록 진행중" 추정 **3~4건** (월 22.6건 × 5일 ÷ 30일)

**조인은 실증했다.** 15건을 RTDB에 개별 조회해 전부 사업명까지 일치함을 확인했다.

---

## 4. 화면 설계

### 4.1 배치 — 도메인 하위 서브탭 (확정)

```
사이드바
──────────────────────────
 콜센터 운영 위탁            ← 도메인
   ├ 공고 리스트             ← 기존
   └ 사전규격 리스트  ③      ← 신규 (뱃지 = 의견마감 임박 건수)
 AX / BPR / ISP              ← 도메인
   ├ 공고 리스트
   └ 사전규격 리스트  ①
 설정
```

**최상위 탭이 아닌 이유.** 현재 사이드바 최상위는 "데이터 종류"가 아니라 **사업 도메인**(콜센터 / AX·BPR·ISP)으로 나뉘어 있다. 사전규격은 두 도메인에 **걸치는 단계(stage)** 라서, 최상위에 두면 도메인이 한 목록에 섞여 화면 안에서 다시 나눠야 한다. `공고 리스트`가 이미 서브탭이므로 같은 층위에 넣으면 구조가 깨지지 않는다.

**입찰공고 목록과 합치지 않는 이유.**

| | 사전규격 | 입찰공고 |
|---|---|---|
| 사용자 행동 | **의견 제출** (규격 변경) | **입찰 참여** |
| 기한 | 의견마감 (중앙값 5일) | 입찰마감 |
| 핵심 필드 | 배정예산, 규격서 파일, 담당자 | 낙찰금액, 개찰업체, 유찰사유 |

1. **행동이 다르다** — 목적 자체가 다르다
2. **컬럼이 안 맞는다** — 사전규격엔 낙찰금액·개찰업체가, 입찰공고엔 의견마감일이 없다. 합치면 행마다 절반이 빈칸
3. **중복으로 보인다** — 사전규격의 91%가 공고로 이어져 같은 사업이 2행이 된다
4. **긴급 건이 묻힌다** — 의견마감 임박은 상시 3~4건. 933건 목록에 섞으면 안 보인다

### 4.2 분리의 단절은 3가지로 메운다

**① 대시보드 지표 카드 4번째 칸** — 사전규격 탭에 들어가지 않아도 홈에서 보인다

```
┌───────────────┬───────────────┬───────────────┬────────────────────┐
│ 연간 총 사업금액│ 연간 공고 수  │ 낙찰 완료     │ ⏰ 의견등록 마감 임박│
│ 3990.5억 원   │ 182 건        │ 37 건 (20%)   │ 3 건  최단 D-2      │
└───────────────┴───────────────┴───────────────┴────────────────────┘
```

- 임박 건이 있으면 빨간 아이콘, 없으면 회색 처리(0건)
- 다른 지표 카드와 동일하게 **라벨 1줄 + 값 1줄**만 둔다. 공고명은 넣지 않는다
  (누르면 목록으로 이동하므로 카드에서 미리 보여줄 필요가 없다)
- **카드를 누르면 사전규격 탭의 '의견중' 필터로 바로 진입**한다.
  목록은 **마감 임박 순**으로 정렬되어 모아 볼 수 있다.
- 사이드바 '사전규격' 서브탭으로 들어오면 '전체' 필터로 열린다.

> 초안에서는 사전규격 탭 안에 상단 배너로 두었으나 **지표 카드로 옮겼다.**
> 배너는 사전규격 탭에 들어가야만 보여서, 정작 놓치면 안 되는 알림이
> 이미 그 화면을 열어본 사람에게만 노출되는 모순이 있었다.

**② 사전규격 상세 → 입찰공고** : "이 건은 공고 2건으로 전환됨" → 기존 공고 상세로 이동
**③ 입찰공고 상세 → 사전규격** : "사전규격 있었음 (8일 전 접수)" ← 역링크

> 흐름은 **목록이 아니라 상세에서** 보여준다. 목록은 "지금 뭘 해야 하나"를, 상세는 "이 건이 어떻게 흘러왔나"를 보는 곳이다.

### 4.3 사전규격 탭 구성

```
┌──────────────────────────────────────────────┐
│  ⏰ 의견등록 진행중  3건        ← 최상단 고정  │
│  ├ D-2  보건복지상담센터 AX기반 ISP  2.3억    │
│  └ D-4  ○○시 콜센터 위탁운영       8.1억    │
├──────────────────────────────────────────────┤
│  [전체][의견중][공고 대기중][공고전환됨][SW]  │
│  검색 ▢                  정렬: 접수일 ▼       │
├──────────────────────────────────────────────┤
│  ⬤사전규격  품명 · 발주기관 · 배정예산        │
│             의견마감 D-2 뱃지                  │
│             🔗 연결된 입찰공고 2건             │
│  ⬤발주계획  품명 · 발주기관 · 발주예정 26-12  │
│             부서/담당자/연락처                 │
│             공고 대기중                        │
└──────────────────────────────────────────────┘
```

- D-3 이내 빨강, D-7 이내 주황. 마감된 건은 흐리게 (숨기지 않음 — 공고 전환 추적에 필요)
- 출처 뱃지로 두 소스를 병합. 발주계획에만 있는 37건을 여기서 흡수
- `activeTab` 유니온에 `'pre-spec'` / `'ax-pre-spec'` 추가

### 4.4 메일 알림

기존 수집 알림 메일에 섹션을 추가한다. **별도 메일을 만들지 않는다.**

```
═══════════════════════════════
⏰ 의견등록 마감 임박 (D-3 이내)
═══════════════════════════════
• [D-2] 보건복지상담센터 AX기반 ISP 수립 (2.3억)
    발주: 보건복지부 / 마감: 2026-03-31 23:59
    규격서: https://www.g2b.go.kr/...
```

> **수집 주기가 의견 창보다 짧아야 한다.** 의견 창이 중앙값 5일, 현재 수집은 1일 주기이므로 충분하다. 주기를 늘리면 기회를 놓친다.

---

## 5. "다음 예정 입찰 정보" 위젯 개편

### 5.1 현재 로직과 한계

```ts
// src/hooks/useBids.ts:70-102
if (bid['용역기간(개월)'] > 0) {
  for (let i = 1; i <= 3; i++)
    predDate.setMonth(predDate.getMonth() + serviceMonths * i)
}
```

`용역기간(개월)`을 수동 입력받아 1·2·3배를 더하는 순수 산술이다.

**[정정] 수동입력 실태 — 예상보다 양호하다.**
"입력하는 사람이 사실상 없다"는 우려가 있었으나 실측 결과 **933건 중 457건(49%)** 에 값이 있다. 분포도 합리적이다 (12개월 138건 · 24개월 107건 · 36개월 24건). 다만 **나머지 51%는 위젯에서 통째로 사라진다.**

**남은 실제 약점 3가지**

1. **51%가 예측 대상에서 제외** — 용역기간이 비면 아예 안 나온다
2. **1·2·3차를 무조건 생성** — 3차는 최대 3년 뒤인데 1차와 같은 비중으로 섞인다
3. **종료된 사업도 영원히 예측** — 재입찰되지 않을 사업이 계속 남는다

### 5.2 [정정] 발주계획으로 "대체"하면 안 된다

검토 중 "발주계획 기반으로 바꾸는 게 현실적"이라는 제안이 있었다. **실측 결과 대체는 손해다.**

| | 현재 예측 | 발주계획 기반 |
|---|---|---|
| 내다보는 기간 | **1~3년** | **최대 4개월** |
| 미래 예정 건수 | 457건 × 최대 3 | **18건뿐** |
| 정확도 | 산술 추정 | 발주기관 등록 |

발주계획 346건 중 **앞으로 발주 예정인 건은 18건**이다 (1개월 후 4 · 2개월 4 · 3개월 4 · 4개월 6). 대체하면 위젯이 거의 비고 4개월 이후가 사라진다.

또한 발주계획의 업무유형은 **신규(단기) 243건 / 장기 28건** — 콜센터 용역은 대부분 단년 계약이라 매년 재발주된다. **"용역기간만큼 뒤에 재입찰된다"는 현재 가정은 이 시장에서 실제로 맞다.**

### 5.3 결론 — 예측 단일 레이어 유지 (확정 레이어 철회)

```
다음 예정 입찰 정보
  ○ 예측  2027-03  유해화학물질 콜센터 운영 (2차)  [추정주기]  3.6억
  ○ 예측  2027-06  학교안전콜센터 위탁운영 (2차)              1.4억
```

**확정 레이어를 한 번 넣었다가 뺐다.** 발주계획 기반 '확정' 항목을 위젯 상단에
붙여 봤으나 **목록이 너무 길어져** 위젯의 목적(다음에 뭐가 오나 한눈에)이 흐려졌다.

- 위젯은 **예측만** 둔다. 확정 정보가 필요하면 사전규격 탭에서 본다
  (발주계획은 그 목록에 '공고 대기중'으로 이미 들어가 있다)
- **3차 예측은 목록에서 제외**해 길이를 줄였다 (최대 3년 뒤라 신뢰도도 낮다)
- 발주계획이 **신규 사업**을 잡아 준다는 점은 유효하지만, 그 값은 위젯이 아니라
  사전규격 탭에서 취한다

### 5.4 용역기간 자동 유도로 커버리지 확대

비어 있는 51%를 **공고 이력에서 자동 유도**한다. 실측으로 타당성을 확인했다.

사업명을 정규화(연도·차수·괄호 제거)해 묶고, **2개월 이내 연속 건은 재공고로 병합**한 뒤 간격을 계산했다.

| 지표 | 값 |
|---|---|
| 주기 산출 가능 사업 | 110개 (143쌍) |
| 주기 중앙값 | **정확히 12개월** |
| 12개월(±1) 비율 | **59%** |
| 상위 분포 | 12개월 51 · 11개월 21 · 13개월 13 · 10개월 9 |

수동입력 분포(12개월 138 · 24개월 107)와 **자동 유도 결과가 일치**한다.

> **재공고 병합이 필수다.** 병합 전에는 최빈 간격이 1개월(70쌍)로 나온다 — 재공고를 연간 주기로 오인한 결과다. 병합 후 12개월이 최빈이 된다.
> 예: `2023-04 → 2024-04 → 2025-04 → 2026-04 → 2026-05 → 2026-06`
> 앞 4건은 연간 주기, 뒤 2건은 재공고다.

**우선순위**: 수동입력 → 이력 자동유도 → 기본 12개월(신뢰도 낮음 표기)

### 5.5 신뢰도 표기

| 근거 | 표기 |
|---|---|
| 발주계획 존재 | **확정** |
| 수동입력 용역기간 | 예측 (높음) |
| 이력 자동유도 | 예측 (보통) |
| 기본 12개월 | 예측 (낮음) |

3차 예측은 목록에서 제외하고 상세에서만 노출한다. 최대 3년 뒤라 신뢰도가 낮다.

---

## 6. 구현

### 6.1 데이터 흐름 — 기존 패턴 그대로

```
GitHub Actions (일 1회)
  └ prespec_collector.py              ← 신규. ax_collector.py 복제 기반
      ├ RTDB /search_keywords 에서 키워드 로드      (기존 함수 재사용)
      ├ 사전규격  getPublicPrcureThngInfoServcPPSSrch × 16
      ├ 발주계획  getOrderPlanSttusListServcPPSSrch × 16
      │            (콜센터 13 + AX/BPR/ISP 3)
      └ Firestore  pre_spec_list / order_plan_list  업서트
                    ↓
server/index.js   GET /api/firestore/pre-specs      ← 신규 라우트
                    ↓
src/hooks/usePreSpecs.ts  →  src/components/PreSpecFinder.tsx
```

기존 `ax_collector.py` → `bid_pblanc_list` → `/api/firestore/bids` → `useFirestoreBids` 와 **동일한 4단 구조**다. 새 개념을 도입하지 않는다.

### 6.2 저장소 — [정정] Firestore → RTDB

**Realtime Database** 를 쓴다.

| 경로 | 키 | 예 |
|---|---|---|
| `/pre_specs` | `bfSpecRgstNo` | `R26BD00175439` |
| `/order_plans` | `orderPlanUntyNo` | `3-1-2016-8781000-000051` |

> **초안에서는 Firestore 로 갔다가 되돌렸다.** "AX 수집과 동일 패턴이라 서버 라우트를
> 재사용할 수 있다"는 이유였는데, **과금 모델을 검토하지 않은 판단이었다.**
>
> | | Firestore | RTDB |
> |---|---|---|
> | 과금 기준 | **문서 읽기 건수** | 전송량 |
> | 우리 데이터 | 1,387건 → 요청당 1,387 read | 약 1.5MB |
> | 무료 한도 | 50,000 read/일 | 1GB 저장 / 10GB 월 전송 |
>
> 프론트가 전량을 받아 필터하는 구조라 페이지 1회 열 때 2,774 read 가 나갔고,
> **하루 18회면 한도가 소진된다.** 실제로 소진시켜 사전규격은 물론 같은
> 프로젝트를 쓰는 AX 입찰공고 화면까지 500 이 됐다.
>
> RTDB 로 옮기면 **클라이언트가 직접 읽어** 서버 라우트·캐시 계층이 통째로 사라진다.
> `/bids` 가 이미 그 방식이고, 그래서 장애 중에도 콜센터 화면만 멀쩡했다.
>
> "RTDB 의 연/월 중첩 구조가 부적합하다"는 것도 이유가 못 된다.
> `/pre_specs/{id}` 같은 평면 구조로 두면 그만이다.

**RTDB 보안 규칙에 경로를 추가해야 한다.** 규칙에 없는 경로는 읽기가 거부된다.

```json
"pre_specs":   { ".read": true, ".write": "auth != null" },
"order_plans": { ".read": true, ".write": "auth != null" }
```

수집기는 서비스 계정(admin SDK)이라 `.write` 제한을 우회한다.
AX 입찰공고(`bid_pblanc_list`)는 아직 Firestore 에 남아 있다.

### 6.3 수집 파라미터 (실측 검증됨)

```python
BASE_SPEC = "https://apis.data.go.kr/1230000/ao/HrcspSsstndrdInfoService"
BASE_PLAN = "https://apis.data.go.kr/1230000/ao/OrderPlanSttusService"

# 사전규격
{"ServiceKey": unquote(BID_API_KEY), "type": "json",
 "pageNo": 1, "numOfRows": 999,
 "inqryDiv": "1",                        # 1 = 접수일시
 "inqryBgnDt": ..., "inqryEndDt": ...,   # 최대 365일
 "prdctClsfcNoNm": keyword}              # 부분일치 확인됨

# 발주계획
{"ServiceKey": unquote(BID_API_KEY), "type": "json",
 "pageNo": 1, "numOfRows": 999,
 "orderBgnYm": "202501", "orderEndYm": "202712",
 "inqryBgnDt": ..., "inqryEndDt": ...,
 "bizNm": keyword}
```

**호출량**: 키워드 16개(콜센터 13 + AX 3) × 2서비스 = **일 32건**. 한도의 1.6%.
**초기 백필**: 키워드당 1~2페이지 → 1회성 64건 내외.

### 6.3.1 도메인 구분

사이드바가 도메인(콜센터 / AX·BPR·ISP)으로 나뉘므로 수집도 도메인을 태깅한다.

| 도메인 | 키워드 출처 | 건수(사전규격/발주계획) |
|---|---|---|
| `callcenter` | RTDB `/search_keywords` (13개) | 617건 |
| `ax` | 코드 고정 `AX` `BPR` `ISP` | 776건 |

- `_domains` 는 **배열**이다. 한 건이 양쪽에 걸칠 수 있다 (실측 6건).
  예: `보건복지상담센터 AX기반 정보전략계획 수립` → `callcenter` + `ax`
- **영문 약어는 단어 경계로 재검증한다.** API 부분일치가
  `Axial` `AXIS` `Maxwell` `Taxonomy` `BLUEMAX` 를 잡아 온다.
  실측 제외: 사전규격 AX 14 · BPR 2 · ISP 3 / 발주계획 AX 6 · BPR 5 · ISP 5

### 6.4 반드시 지킬 구현 세부 (실측으로 확인된 함정)

**① `bidNtceNoList` 형식이 서비스마다 다르다** ← 놓치면 조인율 0%

| 서비스 | 값 | 길이 |
|---|---|---|
| 사전규격 | `R25BK01024415` | 13자 (공고번호만) |
| **발주계획** | `R26BK01648198000` | **16자** (공고번호 + 차수 3자, 구분자 없음) |
| Firestore 문서 ID | `R26BK01649424-000` | 하이픈 구분 |
| RTDB 키 | `R26BK01256644` | 순수 공고번호 → 그대로 조인 |

```python
def norm_bid_no(n: str) -> str:
    n = n.strip()
    return n[:-3] if len(n) == 16 and n[-3:].isdigit() else n.split("-")[0]
```

실제로 정규화 전 **0%** → 후 **297/297 (100%)** 였다.

**② `https` 필수** — `http` 는 80포트 무응답으로 30초 타임아웃

**③ 에러 응답 루트 키가 3종**

| 상황 | 루트 키 |
|---|---|
| 정상 | `response` |
| 파라미터 오류 | `nkoneps.com.response.ResponseError` |
| 인증 실패 | `OpenAPI_ServiceResponse` (HTTP 403) |

`payload["response"]` 직접 접근 시 에러에서 `KeyError` 가 난다.

**④ 조회기간 365일 초과 금지** — 366일부터 `resultCode 07`. 백필은 365일 단위 청크

**⑤ `bfSpecRgstNo` 는 문자열** — 공식 문서는 숫자 10자리(`356759`)지만 실제는 `R26BD00175439` 13자 영숫자

**⑥ `inqryDiv` 의미가 계열마다 다름** — PPSSrch는 `1`=접수일시/`3`=참조번호, 기본 op는 `1`=등록일시/`3`=변경일시

### 6.5 업무구분 범위

**용역(`Servc`)만 구현한다.** 기존 대시보드가 용역 전용이고, 키워드 성격상 물품·공사·외자는 거의 잡히지 않는다. 필요해지면 오퍼레이션명만 바꿔 추가하면 된다 (호출량 4배 → 일 104건, 여전히 여유).

---

## 7. 단계

| 단계 | 범위 | 상태 | 커밋 |
|---|---|---|---|
| **1** | 수집기 + 백필 | ✅ 완료 | `c49b272` (main) |
| **2** | 조회 API + 서브탭 | ✅ 완료 | `1906ff2` |
| **3** | 의견마감 알림 (배너 + 메일) | ✅ 완료 | `c49b272` / `1906ff2` |
| **4** | 공고 양방향 연결 | ✅ 완료 | `ed9adf7` |
| **5** | 예정입찰 위젯 개편 | ✅ 완료 | `f4e6d09` |

### 구현 후 실측

| 항목 | 값 |
|---|---|
| Firestore 적재 | 사전규격 271건 / 발주계획 346건 |
| `/api/firestore/pre-specs` | 617건 반환 |
| 공고 연결 가능 | 사전규격 249/271 · 발주계획 241/346 |
| 예측 커버리지 | **49% → 61%** (자동유도로 117건 신규 확보) |
| 유도 주기 | 110개 사업, 중앙값 12개월 |

---

## 8. 확정된 결정

| 항목 | 결정 |
|---|---|
| 배치 | **도메인 하위 서브탭** (입찰공고와 분리) |
| 발주계획 | **별도 탭 없이 사전규격 목록에 병합** |
| 업무구분 | **용역만** 우선 |
| 예정입찰 위젯 | 대체 아님. **예측 단일 레이어 + 자동유도로 커버리지 확대** |
| 저장소 | Firestore (`pre_spec_list`, `order_plan_list`) |

## 9. 결정 완료 항목

1. **마감 임박 기준** — **배너 D-7 / 메일 D-3** 으로 분리

   > 초안에는 "D-3 배너·메일 공통"으로 적었으나 **배너는 D-7로 넓혔다.**
   > 의견 창이 중앙값 5일이라 D-3만 보면 창이 열린 초반 절반을 놓친다.
   > 메일은 매일 오는 푸시라 더 좁게(D-3) 유지한다. 성격이 다른 채널이다.
   >
   > 기준값은 `usePreSpecs.ts` 의 `IMMINENT_DAYS` 상수 한 곳에서 바꾼다.
   >
   > 색은 가장 급한 건에 맞춘다 — D-3 이내 빨강, D-7 이내 주황.
   > 상자 색을 빨강으로 고정하면 여유 있는 건만 남았을 때 신호가 무뎌진다.
2. **`민원센터` 키워드** — **유지**. 사전규격은 0건이지만 **입찰공고에 5건** 있어
   (`'25년 손실보상 민원센터 운영 용역` 등) 제외하면 입찰공고 쪽에서 손해다
3. **3차 예측 노출** — **목록에서 제외**. 최대 3년 뒤라 1차와 같은 비중은 과하다

---

## 부록 — 초안에서 뒤집힌 가정

투명성을 위해 기록한다. 둘 다 실측 전 추론이었고, 실측이 반박했다.

| 초안 가정 | 실측 결과 |
|---|---|
| 발주계획은 연초 등록 → **수개월 선행** | 게시월 = 발주예정월이 74%. 사전규격보다 먼저인 경우 **21%뿐**. 리드타임 7일로 사전규격(8일)보다 **짧다** |
| 용역기간 수동입력을 **거의 안 한다** | 933건 중 **457건(49%)** 입력됨. 분포도 합리적(12개월 138 · 24개월 107) |

첫 번째 때문에 발주계획을 **별도 탭 → 병합**으로 바꿨고,
두 번째 때문에 예정입찰 위젯을 **전면 교체 → 2층 확장**으로 바꿨다.
